
name: Update CSV


on:
  # push:
  #   branches:
  #     # - main
  workflow_dispatch:  # Added workflow_dispatch trigger
  schedule:
    - cron: '0 9 * * *'  # Every day at 9:00 AM UTC

permissions:
  contents: write

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Display Directory Contents
      run: ls

    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Run main.py
      run: |
        python -u main.py --openai_api_key ${{ secrets.OPENAI_API_KEY }} --CSV_PATH './trending_repositories_summary.csv' --ClassName 'Classification' --url 'https://github.com/trending/python?since=daylie'

    - name: Commit and Push Changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "Update trending repositories summary" || echo "No changes to commit"
        git push